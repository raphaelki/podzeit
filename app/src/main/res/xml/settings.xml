<?xml version="1.0" encoding="utf-8"?>
<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android"
    android:title="Settings">
    <PreferenceCategory android:title="Preferences">
        <CheckBoxPreference
            android:defaultValue="false"
            android:key="@string/shared_pref_remove_after_playback_key"
            android:layout="@layout/custom_settings_layout"
            android:summary="Remove episode from playlist after playback"
            android:title="Remove after playback" />
        <CheckBoxPreference
            android:defaultValue="false"
            android:key="@string/shared_pref_hide_played_key"
            android:layout="@layout/custom_settings_layout"
            android:summary="Hide already played eisodes in the series catalog"
            android:title="Hide played episode in catalog" />
        <ListPreference
            android:defaultValue="15"
            android:entries="@array/skip_seconds_entries"
            android:entryValues="@array/skip_seconds_values"
            android:key="@string/shared_pref_seconds_to_skip_key"
            android:layout="@layout/custom_settings_layout"
            android:summary="Skipping %s seconds on rewind or fast-forward"
            android:title="Seconds to skip" />

    </PreferenceCategory>

</PreferenceScreen>